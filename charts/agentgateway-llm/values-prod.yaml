# Production values for agentgateway-llm
# Переопределения для production окружения
llm:
  cloudru:
    backend:
      # В prod окружении рекомендуется использовать Secret для auth
      auth:
        secretRef:
          name: cloudru-secret
