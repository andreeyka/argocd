# Production values for agentgateway-llm
# Переопределения для production окружения
llm:
  # Список файлов провайдеров для prod окружения
  providerFiles:
    - cloudru.yaml
  defaults:
    namespace: agentgateway-system
