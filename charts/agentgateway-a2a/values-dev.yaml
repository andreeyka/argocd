# Переопределения для dev окружения
a2a:
  agent:
    enabled: true
    # Список файлов агентов для dev окружения
    agentFiles:
      - agent1.yaml
      - agent2.yaml
      - agent3.yaml
    defaults:
      namespace: agentgateway-system
