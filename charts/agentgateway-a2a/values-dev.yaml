# Переопределения для dev окружения
a2a:
  agent:
    enabled: true
    deployment:
      # Пример переменных окружения для dev
      # env:
      #   - name: LOG_LEVEL
      #     value: "debug"
      #   - name: ENVIRONMENT
      #     value: "development"
