# Default values for agentgateway chart
gateway:
  name: agentgateway-proxy
  namespace: agentgateway-system
  gatewayClassName: agentgateway
  listeners:
    - protocol: HTTP
      port: 80
      name: http
      allowedRoutes:
        namespaces:
          from: All
