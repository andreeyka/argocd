apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agentgateway
  namespace: argocd
spec:
  destination:
    namespace: agentgateway-system
    server: https://kubernetes.default.svc
  project: default
  sources:
    # Helm chart для контроллера agentgateway (создает GatewayClass и proxy)
    - repoURL: https://github.com/andreeyka/argocd
      targetRevision: main
      path: kgateway
      helm:
        skipCrds: false
    # Gateway ресурсы из Git
    - repoURL: https://github.com/andreeyka/argocd
      targetRevision: main
      path: charts/agentgateway-gateway
      helm:
        valueFiles:
          - values.yaml
          - values-dev.yaml
    # LLM ресурсы из Git
    - repoURL: https://github.com/andreeyka/argocd
      targetRevision: main
      path: charts/agentgateway-llm
      helm:
        valueFiles:
          - values.yaml
          - values-dev.yaml
    # A2A агент из Git
    - repoURL: https://github.com/andreeyka/argocd
      targetRevision: main
      path: charts/agentgateway-a2a
      helm:
        valueFiles:
          - values.yaml
          - values-dev.yaml
    # MCP серверы из Git
    - repoURL: https://github.com/andreeyka/argocd
      targetRevision: main
      path: charts/agentgateway-mcp
      helm:
        valueFiles:
          - values.yaml
          - values-dev.yaml
    # JWT аутентификация из Git
    - repoURL: https://github.com/andreeyka/argocd
      targetRevision: main
      path: charts/agentgateway-jwt-auth
      helm:
        valueFiles:
          - values.yaml
          - values-dev.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
